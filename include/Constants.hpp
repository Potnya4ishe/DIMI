#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <stdlib.h>

const size_t LINE_LIMIT = 64;

const char SERVICE_SYMBOLS[] = {'+', '-', '/', '*', '=', \
'.', ',', '!', ':', ';', '(', ')', '>', '<', '{', '}'};

const char* const KEYWORDS[] = {"if", "else", "while", "do", "true", "false"};

const char* const FUNCTIONS[] = {"sin", "cos", "log", "exp"};

#endif